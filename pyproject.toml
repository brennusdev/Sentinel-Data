[project]

name = "sentinel-data"

version = "2.0.0"

description = "Real-time data ingestion and analytics platform"

requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "psycopg[binary]",
    "pydantic",
    "alembic",
    "celery",
    "redis"
]


[project.optional-dependencies]

dev = [
    "pytest",
    "httpx",
    "ruff"
]


[tool.pytest.ini_options]

testpaths = [
    "tests"
]


[tool.ruff]

line-length = 88

target-version = "py312"